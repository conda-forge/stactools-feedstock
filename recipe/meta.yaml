{% set name = "stactools" %}
{% set version = "0.4.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/stactools-{{ version }}.tar.gz
  sha256: 1c327ab50a9db8d60b1639c7a3bb28f3bff81ab62072056b9b5948d5fe4ce1d5

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  entry_points:
    - stac = stactools.cli.cli:run_cli

requirements:
  host:
    - pip
    - python >=3.8
  run:
    - aiohttp >=3.8
    - click >=8.1.3
    - fsspec >=2021.7
    - lxml >=4.6
    - numpy >=1.21.0
    - pyproj >=3.0
    - pystac >=1.2
    - python >=3.7
    - rasterio >=1.2.9
    - requests >=2.20
    - shapely >=1.6
    - stac-check >=1.3.2
    - stac-validator >=3.1.0

test:
  imports:
    - stactools
  commands:
    - pip check
    - stac --help
  requires:
    - pip

about:
  home: https://github.com/stac-utils/stactools
  summary: Command line tool and Python library for working with STAC
  license: Apache-2.0
  license_file: LICENSE
  doc_url: https://stactools.readthedocs.io/en/stable/
  dev_url: https://github.com/stac-utils/stactools

extra:
  recipe-maintainers:
    - gadomski
